name: currency_app_st_getx
description: Test app for st.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  get: ^4.5.1
  rxdart: ^0.27.3
  shared_preferences: ^2.0.9
  dio: ^4.0.4
  pretty_dio_logger: ^1.2.0-beta-1
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true